import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Подними свою жопу и делай',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
